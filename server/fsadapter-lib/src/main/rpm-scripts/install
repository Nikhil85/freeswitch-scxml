# installScriptlet
# This script is run as a part of installing the package. 
# It corresponds to the %install tag in the spec file.



%ifos linux

#  Install init files
%{__install} -D -n 0755 ${prefix}/bin/tivrbridge.init.redhat.sh /etc/init.d/tivrbridge

# Add the sysconfiguration file
%{__install} -D -m 744 tivrbridge.sysconfig $RPM_BUILD_ROOT/etc/sysconfig/tivrbridge

%endif
